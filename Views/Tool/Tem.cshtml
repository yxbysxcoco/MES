@using SQ_DB_Framework.Entities
@model EntityBase

<div>
    @Html.Partial("Form")
    @Html.Action("Table", "Table", new { entity = Model})
    @Html.Action("Index","Search",new { entity = Model})
</div>
